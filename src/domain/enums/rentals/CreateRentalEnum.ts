export enum CreateRentalEnum {
  CAR_IS_UNAVAILABLE_ERROR = 'Car is unavailable!',
  USER_ALREADY_HAS_AN_UNFINISHED_RENT_ERROR = 'User already has an unfinished rent!',
  MINIMUM_HOURS_TO_RETURN = 24,
  MINIMUM_HOURS_TO_RETURN_ERROR = 'A rent must have a minimum of 24 hours!',
  MINIMUM_DAILY = 1,
}
